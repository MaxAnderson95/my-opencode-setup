# Ultra

Session-local proactive delegation toggle, with a TUI indicator and `/ultra` command. The plugin respects the user's delegation instructions and permissions.

This copy originates from R44VC0RP's plugin collection and preserves the local version used by Max. Max explicitly selected it for maintenance in this repository.

https://github.com/R44VC0RP/plugins/tree/main/packages/ultra

Install `github:MaxAnderson95/my-opencode-setup#plugin-ultra`. Publish source changes through `scripts/publish-plugins.sh`, then update the configured package in OpenCode.
